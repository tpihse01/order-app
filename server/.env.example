# 서버 환경 변수 예시 파일
# 실제 배포 시 Render 대시보드에서 환경 변수로 설정하세요

NODE_ENV=production
PORT=10000

# PostgreSQL 데이터베이스 연결 정보
# Render PostgreSQL 서비스의 Internal Database URL을 사용하거나
# 개별 변수로 설정할 수 있습니다
DB_HOST=your-database-host
DB_PORT=5432
DB_NAME=order_app
DB_USER=your-database-user
DB_PASSWORD=your-database-password
